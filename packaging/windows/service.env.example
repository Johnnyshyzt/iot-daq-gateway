# 复制本文件为 service.env 后填写。不要把真实密码提交进 git，也不要写进 YAML。
# install-service.bat 会把这些变量写入 Windows 服务 IotDaqGateway 的环境。
# run-console.bat 启动前台进程时也会加载同一份文件。
# 名称必须与页面里的 usernameFromEnv / passwordFromEnv 一致，默认是：
#   MQTT_USER
#   MQTT_PASSWORD
# 值为空的行会被忽略。修改后需重新以管理员运行 install-service.bat 并重启服务。
# STUDIO_ACCOUNT_MODE 由脚本强制为 field，一般不用在这里填写。

MQTT_USER=
MQTT_PASSWORD=
