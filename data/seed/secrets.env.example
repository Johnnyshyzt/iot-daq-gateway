# 不要提交真实密钥。Studio / YAML 只保存变量名 usernameFromEnv / passwordFromEnv。
# Windows 现场包请把值写在安装目录的 service.env（由 service.env.example 复制），
# 再以管理员运行 install-service.bat。不要把密码写进 YAML。
# MQTT_USER=
# MQTT_PASSWORD=
